# ARSENAL FRONT — CLAUDE.md（Claude Code 向け作業規約）

> 対象：Claude Code。Codex CLI 向けの `AGENTS.md` と**同じ内容**に保つこと（この行以外）。
> 作成：2026-09-20（大方針変更に合わせて新設）

## 1. 作業開始時に必ず読むもの（4 点セット）
作業に入る前に、次の 4 つを必ず読むこと。会話の記憶ではなく、この 4 つが正しい情報源。
1. `PLAN.md` — やりたいこと（開発者の意図）
2. `SPEC.md` — 仕様の正本。**§0（2026-09-20 大方針変更）が最優先**
3. `TODO.md` — タスクの状態。「現在地」から読む
4. `KNOWLEDGE.md` — 過去のハマりどころ。同じ問題を二度踏まない

必要に応じて `README.md`、`docs/DATA_STRUCTURE.md`（保存可能なデータ構造の方針）も読む。

## 2. 開発者について
- 開発者（Kohei）はプログラミング経験がなく、**コードを読めない**
- 説明は必ず **「何をしたら、画面で何が起きるか」という動作結果** で行う
  - 例：「F5 で起動すると、灰色の床の上に箱型のロボットが立っています。W キーで前に歩きます」
- 専門用語は平易な日本語に言い換えるか、注釈を付ける
- 開発者が確認する手順（どのキーを押し、何が見えたら成功か）を毎回添える

## 3. 仕様にないことは推測で作らない
- `SPEC.md` に書かれていないこと、[TBD] のことは、**推測で決めずに開発者へ質問する**
- 仕様が決まったら `SPEC.md` に書き、日付と理由を残す。旧仕様は消さずに「変更前」「保留」として残す
- 「できました」と報告する前に、実際に動くことを確認する。確認できない場合はそう正直に書く

## 4. 技術面の前提
- エンジン：**Godot 4.6.3 stable（3D）＋ GDScript**
  - 使う関数・機能は、この版の公式ドキュメントで存在を確かめてから使う
  - 実行ファイル：`C:\Users\tkmuh\AppData\Local\Microsoft\WinGet\Packages\GodotEngine.GodotEngine_Microsoft.Winget.Source_8wekyb3d8bbwe\Godot_v4.6.3-stable_win64_console.exe`
  - 無人での起動確認：`--headless --path . --import` の後に `--headless --path . --quit-after 60`（KNOWLEDGE.md 参照）
- 設計上の必須ルール（SPEC §0.3）：操縦可能な物の共通化／乗り物は操縦席＋乗員の構造／カメラ視点を後から追加できる構造／データは保存（シリアライズ）可能に

## 5. 進め方
- 1 ステップずつ実装し、ステップごとに開発者の確認を待ってから次へ進む
- 進捗は `TODO.md` に、解決したエラー・ハマりどころは原因と対処を `KNOWLEDGE.md` に記録する
- コミット・push は開発者の承認を得てから行う
- `CLAUDE.md` を更新したら `AGENTS.md` も同じ内容に更新する
