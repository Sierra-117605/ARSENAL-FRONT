# ARSENAL FRONT — KNOWLEDGE.md（学び・ナレッジ）

> **プロジェクト名：ARSENAL FRONT**
> リポジトリ：https://github.com/Sierra-117605/ARSENAL-FRONT

## このドキュメントの位置づけ
- 一度ハマったことに二度とハマらないための知見の蓄積
- 「何で詰まったか」「どう解決したか」をセットで残す
- 実装が始まる前の現時点では、(1) 確定した重要決定の要約、(2) 着手前から分かっている注意点 を記録
- ハルシネーション防止のため、**未確定のことを確定事項として書かない**。推測は「推測」と明記する

---

## 1. 確定した重要決定の早見表
詳細は SPEC.md。ここは「結論だけ」を素早く思い出すための表。

> **【2026-09-20 大方針変更】** 下表の上 5 行が最新。それより下の行のうち【保留】【変更前】と付いたものは旧方針（SPEC §0.6）。

| 項目 | 決定 | 根拠 |
|---|---|---|
| ゲームの中心（2026-09-20） | プレイヤーが機体を直接操縦する 3D アクション | SPEC §0.1 |
| 描画（2026-09-20） | 3D（Godot 4 のまま） | SPEC §0.1 |
| 視点（2026-09-20） | Phase 1 は三人称のみ。将来コックピット視点を追加・切替 | SPEC §0.1, §0.3 |
| 新 Phase 1（2026-09-20） | ロボット 1 機を 3D 空間で操縦できる | SPEC §0.4 |
| 設計ルール（2026-09-20） | 操縦可能な物の共通化／操縦席＋乗員／カメラ視点追加可能／シリアライズ可能 | SPEC §0.3 |
| プロジェクト名 | ARSENAL FRONT | 正式決定 |
| 世界観 | 現代軍事＋SFメカ混在（MGS/ストレンジリアル系） | SPEC §1.2 |
| 時代設定 | 現代〜近未来兵器のみ（WW2 以前なし） | SPEC §1.2 |
| 兵科 | 歩兵/車両/ロボット/航空機/艦船 の 5 種＋各兵科に区分 | SPEC §1.3 |
| 品質目標 | 3 段階（自分→他人→商用配信） | PLAN §8 |
| マルチプレイ | PvE × P2P、将来実装、設計初期から考慮 | SPEC §2.14 |
| 3D 化【変更前】 | しない（2D 固定、3D は次回作）→ 2026-09-20 に 3D 採用へ変更 | SPEC §2.15 |
| 技術スタック | Godot 4 + GDScript（2026-09-20 に 2D → 3D） | SPEC §2.8 |
| ステータス【保留】 | HoI4 風 8 項目（初期は 5 項目） | SPEC §2.1 |
| ダメージ計算【保留】 | 貫通式（貫通 vs 装甲） | SPEC §2.2 |
| Phase 1 兵科【変更前】 | 装甲戦闘車両 | SPEC §1.6 |
| Phase 1 区分【変更前】 | 主力戦車 / 偵察戦闘車 / 対戦車ミサイル車両 | SPEC §2.18 |
| 車両スロット【保留】 | 固定 6 ＋ 可変モジュール | SPEC §2.18 |
| ターン単位【保留】 | 日単位＋スキップ進行 | SPEC §2.3 A |
| Phase 1 戦闘【変更前】 | ターン制シミュレーション（ログ表示）の自動戦闘 | SPEC §2.3 B |
| 実装ツール | Claude Code ＋ Codex の2つのみ（相互レビュー）<br>※2026-06-19 時点 Codex 未契約。当面 Claude Code 単独で進行 | PLAN §3 |

---

## 2. 着手前から分かっている注意点・落とし穴

### 2-1. マルチプレイ後付けは作り直しになる
- シングル前提でデータ構造を固めると、後から P2P 同期を足すのは事実上の作り直し
- **対策**：最初からユニット・戦闘状態・任務進行をシリアライズ可能／イベント駆動で組む（TODO T1-4）
- 出典：開発方針として確認済み（SPEC §2.14）

### 2-2. 共通基盤を兵科専用に作り込むと他兵科に流用できない
- Phase 1 は車両だが、「ユニット＝区分＋スロット＋パーツ＋ステータス計算」は全兵科共通の枠組み
- 車両特有の概念をハードコードすると、ロボ/航空機/艦船追加時に壊れる
- **対策**：データ構造は汎用に、車両固有部分はデータ（JSON）側に寄せる（TODO T2 系）

### 2-3. 日単位ターンは「空の日の連打」で作業感が出る
- **対策**：イベント発生日まで自動スキップする進行を必ず入れる（HoI4 方式、SPEC §2.3 A）

### 2-4. 数値バランスを最初から完璧にしようとすると進まない
- ステータスもダメージ式も具体数値は [TBD] のまま。動かして調整する前提
- **対策**：仮の数値で動かす → プレイ → 調整、のループ。最初の数値は「とりあえず」で良い

### 2-5. Godot/GDScript は Claude のトレーニング情報が Pygame より少ない可能性
- 出典：SPEC §2.8 の留意点（推測を含む）
- **対策**：実装初期（TODO T1-3）に簡単なサンプルで出力品質を確認しておく。問題が出たら早期に方針再考

### 2-6. 自分はコードを読めない → 検証手段を別に用意する必要
- コードの正しさを直接レビューできないため、「動かして結果を見る」で検証するしかない
- **対策1**：ステータス計算は手計算と一致するかテスト（TODO T3-3）、戦闘は結果が直感に合うかで判断
- **対策2**：Claude Code と Codex の相互レビュー。一方が書いたコードをもう一方にチェックさせ、人間がコードを読めない弱点を補う
- これは PLAN §9 で挙げた懸念そのもの

---

## 3. 実装中に詰まったこと（ここから随時追記）

### 2026-09-20 .tscn に手書きする Transform3D は「行」の順で並べる（ハマりポイント）
- 状況：N1-1 でカメラを「下向き 15°」にするつもりで .tscn に回転を書いたら、カメラが上向きになりロボットが画面下で切れた
- 原因：.tscn の `Transform3D(a,b,c, d,e,f, g,h,i, 位置x,y,z)` の最初の 9 個は、基底（向き）を **行ごと** に並べたもの。軸ベクトル（列）ごとに並べると回転が逆向きになる
- 解決：9 個を行の順（列順を転置したもの）に書き直した
- 教訓：回転を手書きしたら **必ず画像で見え方を確認する**（下の「画像で確認する方法」）
- 関連 TODO：N1-1

### 2026-09-20 .tscn でノードを指す設定には node_paths の印が要る（ハマりポイント）
- 状況：N1-2 で乗員(Pilot)に「座る操縦席」を `start_seat = NodePath("../Robot/DriverSeat")` と手書きしたら、実行時に空(null)のままでロボットが動かなかった
- 原因：Godot 4 の .tscn では、ノード型の設定値を持つノードの見出しに `node_paths=PackedStringArray("start_seat")` が無いと、NodePath がノードに変換されない（エディタで保存すると自動で付くが、手書きでは付かない）
- 解決：`[node name="Pilot" type="Node" parent="." node_paths=PackedStringArray("start_seat")]` のように見出しへ追加
- 教訓：.tscn を手書きしてノード参照の設定を入れたら、必ず node_paths を付ける。自動テストで「参照が空でないか」まで確認する
- 関連 TODO：N1-2

### 2026-09-20 マウスでカメラを回すときは screen_relative を使う
- Godot 4.6 公式ドキュメント（InputEventMouseMotion）：`relative` は画面の拡大縮小設定（本プロジェクトは stretch_mode=viewport）の影響を受け、解像度で感度が変わる。マウス照準には `screen_relative` が推奨されている
- 採用：`scripts/action/player_input.gd` は `screen_relative` を使用
- マウスの動きの自動テストは `Input.parse_input_event()` で InputEventMouseMotion を送れば、ヘッドレスでも `_unhandled_input` に届く（`tests/test_n1_3_look.gd` で確認済み）
- 関連 TODO：N1-3

### 2026-09-20 テスト用スクリプトの _initialize では、まだ世界に置かれていない
- 状況：`extends SceneTree` のテストの `_initialize()` で `root.add_child()` した直後に `global_position` を設定したら「!is_inside_tree()」エラーで位置が反映されなかった
- 解決：`_initialize()` の中では `global_position` ではなく `position`（親からの相対位置）を使う。親が原点なら同じ値になる
- 関連 TODO：N1-4

### 2026-09-20 照準の先は「カメラから見た画面中央」の地点で決める
- 弾は銃口から「カメラの画面中央の先にある地点」へ向けて飛ばす（肩越し視点でも照準と着弾が一致する）。計算は `scripts/action/player_input.gd` の `_find_aim_point()`
- ハマり：カメラの初期の下向き角度が 8° だと、照準の先が約 25m 先の地面になり、それより遠い物に当たらなかった。初期角度を 3° に変更（地面に当たるのは約 75m 先）
- 関連 TODO：N1-4

### 2026-09-20 消えた(queue_free した)物は null と比べると「等しい」になる
- 状況：N1-5 のテストで、壊れて消えた標的を `current == null` で判定していたため、「全部終わった」と誤判定した
- 教訓：消えたかどうかは `is_instance_valid(x)` で調べる。`== null` は「まだ設定していない」と「消えた」の区別がつかない
- 関連 TODO：N1-5

### 2026-09-20 標的の並べ方：同じ方向に重ねない
- ロボットから見てほぼ同じ方向（約 18°）に手前と奥の標的があると、手前を壊した後も飛んでいた弾が奥に当たる（ゲームとしては正しい動き）
- 配置を変えて解消（標的 3 を (-18,-55) → (-25,-50)）。今後ステージを作るときも、確認用の的は方向をずらす
- 関連 TODO：N1-5

### 2026-09-20 日本語を画面に出す方法（Godot の既定フォントには日本語が無い）
- 既定のフォントでは日本語が豆腐（□）になる。`assets/ui_theme.tres` に **SystemFont**（`font_names = ["Yu Gothic UI", "Meiryo", ...]`）を入れたテーマを作り、画面のルートに割り当てると出る
- 公式ドキュメント（4.6 SystemFont）で `font_names` と `allow_system_fallback` を確認済み
- 3D 空間に浮かぶ文字（Label3D）や `draw_string` は既定フォントを使うので、そこは今も英語表記
- 関連 TODO：N3-2

### 2026-09-20 自動テストが「開発者の保存データ」に影響されていた（ハマりポイント）
- 症状：単体で走らせると合格するテストが、まとめて走らせると落ちる（射撃まわり）。原因を追うと、開発者がガレージで保存した構成（キャノン）をテストのプレイヤー機が読み込んでいた
- さらに悪いこと：テストが後片付けのつもりで保存ファイルを消しており、**開発者が選んだ構成が消えていた**
- 解決：
  1. テストでは `robot.use_saved_loadout = false` にして保存ファイルを読まない（シーンの標準設定のまま使う）
  2. ガレージのテストは、保存ファイルの中身をいったん覚え、終わったら元に戻す
- もう一つの落とし穴：`SceneTree` のテストで `_initialize()` の中で値を入れても、**そのあとに走る `_ready()` に上書きされる**。設定は `_ready` より前に効くもの（`use_saved_loadout`）にするか、最初のフレームで入れ直す
- 教訓：**テストは開発者のセーブデータを読まない・書き換えない**
- 関連 TODO：N4-1

### 2026-09-20 機種ごとの大きさの変え方（N4-2）
- 仮のロボットは全高 4m で組んであるので、**機種の全高 ÷ 4** を倍率にして見た目・当たり判定・操縦席・銃口の位置をまとめて変える
- 当たり判定（カプセル）はノードの拡大ではなく、形の数値（半径・高さ）を書き換える。拡大は物理で不具合が出やすいため
- 四脚は、隠してある後脚 2 本を表示して実現。歩く動きは前脚と逆に振る
- カメラは機体の全高に比例して引く（`ThirdPersonView.base_machine_height = 12` を基準）
- 機種の数値は `data/machine_types.json`。倍率（耐久・速さ・火力・照準距離）で表現する
- 関連 TODO：N4-2

### 2026-09-20 音は自作（合成）で用意した
- 素材を外から持ってこず、`tools/make_sounds.py` で波形を合成して WAV を作る（著作権の問題を避けるため）。作り直しは `python tools/make_sounds.py`
- 7 種類：射撃・着弾・被弾・撃破・足音・勝利・敗北。`scripts/action/sounds.gd` が鳴らす
- 3D の音は「場面の一番上」に置く。機体の子にすると、機体が壊れて消えた瞬間に音が切れる
- 関連 TODO：N4-1

### 2026-09-20 敵の耐久が構成で上書きされていた（ハマりポイント）
- 症状：開発者から「105mm キャノンで 8 発当ててやっと倒せた」と報告。シーンでは敵の耐久を 100 に設定していた
- 原因：敵もプレイヤーと同じ Robot（同じ仕組み）を使っているため、起動時の `apply_loadout()` が **標準の構成の耐久 330** で上書きしていた。330 ÷ 45（当時のキャノン）= 8 発と計算が一致
- 解決：構成が指定されている機体（プレイヤー機、または loadout を明示した機体）だけ性能を入れ替える。指定の無い機体はシーンの設定値をそのまま使う
- 教訓：**同じ仕組みを共有する時は「どちらの設定が勝つか」を決めておく**。テストに「敵の耐久はシーンの値のまま」の確認を追加した
- 関連 TODO：N3-5

### 2026-09-20 Phase 3（組み立て）の作り
- パーツ一覧は **JSON**（`data/robot_parts.json`）。数値の調整はスクリプトを触らず JSON だけ直せばよい
- 読み込みと性能計算は `scripts/action/robot_parts.gd`。加算（耐久）と倍率（速さ・火力・連射）を分けて計算する
- 構成の保存は `user://loadout.json`（`loadout_store.gd`）。プレイヤー機は `use_saved_loadout = true` で起動時に読み込む
- 見た目は「スロット → 変化させる部品」の対応表（`Robot.VISUAL_PARTS`）で大きさと色を変える。敵機の赤塗りとは両立させてある
- 画面の流れ：ガレージ（起動時）→ 出撃 → 戦闘 →（G キー）→ ガレージ
- 関連 TODO：N3-1〜N3-4

### 2026-09-20 Phase 2（戦闘の成立）で決めた数値と作り
- **2026-09-20 Phase 2 完了**：開発者が実機で確認し「問題ない」と判断。自動テスト 8 本すべて合格
- ダメージの単位を統一：弾 1 発 = 10。標的 30（3 発）／自機 300（30 発）／敵 100（10 発）
- 敵の作り：プレイヤーと **同じ機体・同じ道筋**（乗員 → 操縦席 → 乗り物）。違いは乗員が AI（`scripts/action/ai_pilot.gd`）かプレイヤー（`player_input.gd`）かだけ。将来の乗り換え・味方 AI にそのまま使える
- 敵の色は `Robot.body_color` で塗り替える（機体シーンを別に作らない）
- 戦闘の進行役は `scripts/action/battle.gd`（フィールドのルート）。自機の撃破で lose、敵全滅で win、R キーでやり直し
- バランス：敵 3 体 × 射撃間隔 2 秒 × ダメージ 10 = 毎秒 15。自機 300 なら約 20 秒もつ。敵の狙いに 4m のブレを入れて百発百中を避けた
- 物陰は当たり判定のある箱（`scenes/cover_block.tscn`）。**自機の正面に置くと弾の通り道をふさぐ**ので、テストが落ちて気付いた（物陰は右にずらし、標的の練習場は左へ移した）

### 2026-09-20 新 Phase 1 完了（達成サマリ）
- 完了条件 5 点すべて達成し、開発者が PC の実機で確認（立っている／歩ける／カメラを回せる／撃てる／標的が壊れる）
- 作ったもの：`scenes/field_3d.tscn`（3D の場）・`scenes/robot.tscn`（多用途歩行機 12m）・`scenes/target.tscn`・`scenes/bullet.tscn`、`scripts/action/` 配下（操縦可能な物／操縦席／乗員／入力／カメラ土台／三人称視点／武器／弾／標的／照準）
- 自動テスト 5 本（`tests/test_n1_*.gd`）。すべて `RESULT: PASS`
- 仮の値（今後調整）：歩く速さ 10m/秒、マウス感度 0.003、連射間隔 0.25 秒、弾速 300m/秒・射程 1200m、標的 3 発で破壊、脚の振り 22°

### 2026-09-20 .bat から Godot を開く時、%~dp0 の末尾の \ が引用符を壊す（ハマりポイント）
- 状況：`open_in_godot.bat` をダブルクリックしても Godot エディタが開かなかった
- 原因：`--path "%~dp0"` の `%~dp0` は末尾が `\` で終わるため、`"C:\dev\ARSENAL-FRONT\"` の `\"` が引用符のエスケープと解釈され、引数が壊れる
- 解決：`cd /d "%~dp0"` でフォルダへ移動してから `--path "%CD%"` を渡す（`%CD%` は末尾に `\` が付かない）
- ついでに `play_game.bat`（エディタを開かずゲームだけ起動）も追加。Godot が見つからない時はメッセージを出して止まる
- メモ：.bat の中の日本語メッセージはコマンド画面で文字化けするので英語にした
- 関連 TODO：N1-6

### 2026-09-20 自機の大きさ変更の数値（N1-5b → N1-5d で 12m に変更）
- **現在（N1-5d）は 12m**：拡大率 3.0 倍。カメラ 回転中心 10.4m・距離 27.2m・右ずらし 7.6m。標的 6×9×6m。マス目 12m。歩く速さ 10m/秒。以下は 15m 時点の記録
- 拡大率 3.75 倍（4m → 15m）。見た目は `Visual` ノードをまとめて 3.75 倍。当たり判定（カプセル）は拡大せず、半径・高さの数値を直接 3.75 倍にした（物理の形はノードの拡大で大きくしない方が安全なため）
- カメラ：回転中心の高さ 13m・距離 34m・右ずらし 9.5m。弾：秒速 300m・射程 1200m。標的：7.5×11.25×7.5m。地面：1500m 四方、マス目 15m
- 歩く速さは比率どおり（約 22m/秒）にせず 12m/秒にした（重さを感じさせるため。仮値）
- 注意：全部を同じ比率で拡大したので、画面の見た目は拡大前とほぼ同じ。大きさは人間・戦車・建物など大きさの分かる物が並んで初めて伝わる
- 関連 TODO：N1-5b

### 2026-09-20 キー操作の自動テストのやり方
- `tests/` に `extends SceneTree` のスクリプトを置き、`Input.action_press("move_forward")` で「キーを押した」ことにして結果を調べる
- 実行：`Godot_v4.6.3-stable_win64_console.exe --headless --path . --script res://tests/test_n1_2_walk.gd` → 最後に `RESULT: PASS` / `FAIL`
- 新しい class_name を足した直後は先に `--import` を 1 回走らせる（上の項目参照）
- 関連 TODO：N1-2 以降

### 2026-09-20 画面を画像に保存して見え方を確認する方法
- `Godot_v4.6.3-stable_win64_console.exe --path . --write-movie <保存先>/frame.png --quit-after 20`
  - ゲームを 20 フレーム動かし、各フレームを `frame00000000.png` 〜 として保存して終了する（Godot 標準のムービー書き出し機能）
  - `--headless` では描画されないので付けない（ウィンドウが一瞬開く）
- 開発者に見てもらう前に、実装者がこれで見え方を確認できる
- 関連 TODO：N1 系全般

### 2026-06-19 Codex 未契約の暫定運用（重要）
- 状況：PLAN §3 / SPEC §2.8 / docs/REVIEW_FLOW.md は「Claude Code ＋ Codex の相互レビュー」前提だが、Codex はまだ契約していない
- 暫定方針（B案）：Claude Code 単独で実装し、`docs/REVIEW_FLOW.md §4` のチェックリストでセルフレビュー
- 動作検証はユーザーが Godot を起動して目視で行う（コードを読む手段の代替）
- Codex 契約タイミング：他兵科追加やマルチプレイ実装など、Phase 1 より複雑な領域に入る前を推奨
- 関連 TODO：T1-3

### 2026-06-19 Phase 1 形式上クリア (達成サマリ)
- 環境構築 (T1) → データ構造 (T2) → ステータス計算 (T3-1) → アセンブル UI (T5-1〜4) → 自動戦闘 (T6-1/2/4/5) → セーブ (T5-3/T8) → 司令官モード (T7-1/2/3/4) まで実装
- Phase 1 ゴール条件 4 点 (戦車組立 / 5項目ステータス算出 / 自動戦闘 / セーブ) すべて達成
- 開発体制: Claude Code 単独 (Codex 未契約のため B案を継続)
- 残課題: T9-2 ユーザー実機プレイ検証、数値バランス全般、出撃モード (Phase 2)
- 設計判断の記録: SPEC §2.4 (資金+素材) / §2.5 (殲滅×3難易度) は Phase 1 暫定確定として SPEC.md に明記済み
- スクリプト構成 (Phase 1 完了時):
  - `stats.gd` / `parts_catalog.gd` / `unit.gd` (共通基盤)
  - `combat.gd` (自動戦闘)
  - `save_io.gd` (設計セーブ)
  - `game_state.gd` (ゲーム状態 + イベント駆動更新)
  - `missions.gd` (敵設計カタログ + 任務テンプレ)
  - `main.gd` (UI 全体の制御)

### 2026-06-19 Phase 1 任務システムは敵設計を別カタログで持つ
- 設計上の判断：プレイヤーが編集できる `saved_designs` と、任務に登場する敵の設計を**分離**した (`scripts/missions.gd` の `ENEMY_DESIGNS` 定数)
- 理由：
  - プレイヤーが全設計を削除しても敵が消えないようにする
  - 敵設計のバランス調整を別ファイルで管理できる
  - 敵設計 ID と任務テンプレが疎結合
- 任務生成は単純抽選 (`Missions.generate_one`)。難易度の出現頻度などは今後調整余地あり
- 任務結果は GameState の `mission_log` に直近 20 件保持。Phase 1 ではフィルタなし
- 関連 TODO: T7-3, SPEC §2.5

### 2026-06-19 セーブデータの保存先は user:// (res:// ではない)
- 状況：プレイヤーが組んだ設計を JSON で保存するために最初は `res://saves/designs.json` に書く案を検討
- 落とし穴：**`res://` 配下はエクスポートしたゲームでは読み取り専用**。ローカル開発時しか書き込めず、配信版でセーブが壊れる
- 解決：**`user://` を使う** (`user://designs.json`)。Godot が OS ごとに適切な永続フォルダにマップしてくれる
  - Windows: `%APPDATA%\Godot\app_userdata\<project_name>\`
  - Linux: `~/.local/share/godot/app_userdata/<project_name>/`
  - macOS: `~/Library/Application Support/Godot/app_userdata/<project_name>/`
- 教訓：**プレイヤー固有データ (セーブ・設定) は user://、ゲーム同梱データ (パーツ JSON・画像) は res://** と覚える
- 関連 TODO: T5-3, T8
- 関連: docs/DATA_STRUCTURE.md の方針 (シリアライズ可能・イベント駆動) はこの選択でも問題なく成立

### 2026-06-19 Scout 救済: 貫通失敗時の残存ダメ補正を導入
- 課題: 旧仕様の貫通失敗ペナルティ (×0.25 on 選択された攻撃値) だと Scout の対装甲 10 が ×0.4×0.25 = 1 ダメに収束し、装甲相手で機能不全だった
- 解決: 貫通失敗時は **soft_attack × SOFT_DAMAGE_RATIO × RESIDUAL_FACTOR (=0.25)** を「残存ダメ」として通すように変更
  - Scout 対装甲 → 残存 = 35 × 0.5 × 0.25 = 4.4 → 4 ダメ/R (旧 1 ダメから 4 倍)
  - MBT 対装甲 (貫通失敗時) → 25 × 0.5 × 0.25 = 3 ダメ (機銃掃射として継続火力)
- 物理的解釈: 主砲が弾かれても機銃や副武装で薄く削る、装甲機は副武装にも装甲がある
- バランス試算 (3 Scout vs 1 MBT):
  - 3 Scout の毎R合計ダメ: 4×3 = 12
  - MBT vs Scout の毎R: ソフト 25×0.5 = 13 → Scout 1 機を 4R で撃破
  - MBT HP 95 / 12 ≈ 8R → Scout 側がギリギリ勝つペース
- SPEC §2.2 を「Phase 1 暫定確定」に更新済み
- 関連 TODO: T4-4 (バランス調整)
- 関連コミット: (このコミット)

### 2026-06-19 Phase 1 自動戦闘のバランス係数（暫定値、要調整）
- 仮係数：対人ダメ ×0.5, 対装甲ダメ ×0.4, 貫通失敗時さらに ×0.25, 装甲閾値=25
- 3 戦闘デモでの結果:
  - MBT vs ATGM: 4R で先攻 MBT 勝利（同攻撃力同 HP に近く、先攻有利）
  - MBT vs Scout: 4R で MBT 勝利（Scout はほぼダメージ与えられず）
  - ATGM vs Scout: 10R で ATGM 勝利（ATGM の対人攻撃が貧弱なため時間かかる）
- 学び:
  - 数発で決着がつく良いペース（4〜10R）。仮係数は方向性として悪くない
  - **先攻有利が極端**。Phase 2 で機動ステータスを実装し、行動順をシャッフルする際に解決すべき
  - Scout は対装甲攻撃のみで装甲 25 以上には実質ダメージ0（×0.25=1ダメ）。
    Scout に対人攻撃を載せるか、貫通失敗ペナルティを緩めるか要検討
- 関連 TODO: T6-3 (Phase 2 で機動・行動順), T4 (パーツ数値調整)

### 2026-06-19 画面いっぱいに UI を広げる方法（最大ハマりポイント）
- 状況：F5 で起動した画面が、ウィンドウを最大化しても中央の小さい箱に収まったまま広がらなかった。数回の修正を経て解決
- 原因（複数の要因が重なっていた）：
  1. **Node2D + 絶対座標で UI を組んでいた**：ウィンドウサイズが変わってもピクセル位置が固定のため広がらない → 解決：ルートを Control にして anchors_preset=15（フルアンカー）で組む
  2. **Godot 4.4+ の Embedded Game Mode**：F5 実行時にゲームを「エディタ内蔵のデバッグウィンドウ」で表示する新機能。エディタ設定 → 実行 → ウィンドウの配置 → Game Embed Mode を **「Disabled」** に変更しないと、画面伸縮設定が無視される
  3. **stretch_mode=canvas_items + aspect=expand**：UI 拡縮には使えるが、ウィンドウサイズ追従の効きが弱い場面がある → 解決：**`stretch_mode="viewport"` + `stretch_aspect="keep"`** に変更（ピクセルバッファ全体をウィンドウに引き伸ばす、16:9 維持）
- 教訓：
  - **画面表示の Control は最初から `Control` ルート + アンカー** で組む（Node2D は世界に物を置く用）
  - **`stretch_mode="viewport"` は確実に効く**。情報密度を細かく調整する必要が出てきたら `canvas_items` に戻す検討をする
  - **新規 PC で Godot 4.4+ を使う場合は最初に Game Embed Mode を Disabled にしておく**
  - 長文や多数項目を表示するときは **ScrollContainer でラップする** のが定石
- 関連 TODO：T1-2（フォルダ構成・project.godot 設定）、T5（アセンブル UI）以降の全画面実装
- 関連コミット：`2559866`（Control 化）、`a09e5c5`（viewport モード化）、`f42d8e7`（ScrollContainer 追加）

### 2026-06-19 新しい class_name を追加したらインポートが必要（ハマりポイント）
- 状況：`class_name Stats` などを新規追加したスクリプトを書いた直後にヘッドレス実行 (`--quit-after`) したら "Could not find type Stats in the current scope" でパースエラー
- 原因：Godot は **エディタが起動するか `--import` が走るとき** にプロジェクト全体をスキャンして `class_name` のグローバル登録を更新する。新規 class_name はその登録が無いとパースで見つからない
- 解決：`godot --headless --path . --import` を1回挟んでから実行する
- 教訓：**新規スクリプトを追加した直後の検証は必ず `--import` → 実行 の2段** で行う。エディタを開いている場合はファイル保存時に自動再スキャンされるので問題ない
- 関連 TODO：T2 系（class_name を多用するため）

### 2026-06-19 環境構築フェーズの学び（T1-1 〜 T1-4）
- 状況：Godot 4 のインストールから最小プロジェクト作成まで進めた
- 学び・要点：
  - **Godot は winget で導入できる**（`winget install GodotEngine.GodotEngine` で 4.6.3 stable が入った）
  - winget 経由の場合、実体は `C:\Users\tkmuh\AppData\Local\Microsoft\WinGet\Packages\GodotEngine.GodotEngine_*\Godot_v4.6.3-stable_win64.exe` に置かれる
  - PATH のエイリアス（`godot`）は管理者権限なしだと作られないため、シェルで `godot --version` がすぐ通らない場合あり → エディタ起動はスタートメニューから可能
  - **動作検証は `godot --headless --path <project> --quit-after <frames>` で可能**（無人検証に有用）
  - **`godot --headless --path <project> --import` でプロジェクトを最初にインポート**しておくと、`.godot/` キャッシュが生成されてその後の起動が速い
- 関連 TODO：T1-1, T1-3
- 関連ドキュメント：docs/REVIEW_FLOW.md（相互レビュー運用）, docs/DATA_STRUCTURE.md（データ構造方針）

<!--
### [日付] タイトル（例：Godot のセーブで配列が壊れる）
- 状況：何をしていて何が起きたか
- 原因：判明した原因
- 解決：どう直したか
- 教訓：次にどうすれば避けられるか
- 関連 TODO：T?-?
-->

---

## 4. ドキュメント運用のルール（4 点セット）
| ファイル | 役割 |
|---|---|
| PLAN.md | やりたいことの全体像 |
| SPEC.md | 仕様の確定事項と [TBD] |
| TODO.md | タスク管理（リセットしても再開可能） |
| KNOWLEDGE.md | 学び・ハマりどころ（このファイル） |

- 仕様が決まったら SPEC、タスク化したら TODO、詰まったら KNOWLEDGE
- このチャット（claude.ai）では仕様策定のみ。実装は Claude Code と Codex の2ツール（相互レビューで補強）
