# ARSENAL FRONT — TODO.md（タスク管理）

> **プロジェクト名：ARSENAL FRONT**
> リポジトリ：https://github.com/Sierra-117605/ARSENAL-FRONT

## このドキュメントの位置づけ
- Claude Code と Codex が実装するためのタスク管理リスト
- **コンテキストをリセットしても、このファイルを読めば再開できる**ことを最優先
- 各タスクは「何を作るか」が一文で分かる粒度
- 仕様の根拠は SPEC.md の該当セクションを参照（重複定義しない）
- ステータス記号：`[ ]` 未着手 / `[~]` 作業中 / `[x]` 完了 / `[!]` 詰まり中（KNOWLEDGE.md に記録）

---

## 現在地（最初に読む）
- **現在のフェーズ：Phase 1（装甲戦闘車両のみ・司令官モード＋自動戦闘）**
- **技術スタック：Godot 4.6.3 stable（2D モード）+ GDScript**（SPEC §2.8）
- **直近の作業（2026-06-19）**：
  - T1（環境構築）〜 T2（データ構造）〜 T3-1（ステータス計算）〜 T5（アセンブル UI + セーブ管理）〜 T6（自動戦闘）〜 T7-1/2/4（司令官モード基盤）まで実装
  - 司令官タブを最初のタブに、保存済み設計の 3 カラム編集 UI、戦闘デモの総当たり 1v1 が動く状態
  - SPEC §2.4 を「資金 + 素材で確定」に更新
  - `user://designs.json` と `user://gamestate.json` の 2 ファイルで永続化
- **Codex 契約状況**：**未契約**。よって T1-3 の「Codex レビュー」は実施不可
  - 当面の運用：Claude Code 単独実装＋ユーザーによる動作検証で進める（B案）
  - Codex 契約後、それまでに書いた分をまとめてレビュー依頼する
- **次にやること**：
  1. ユーザーが F5 で「司令官」タブの製造 + 任務派遣 + 自動戦闘解決ループを試す
  2. Phase 1 のゴール条件 (本ファイル冒頭) は形式上すべて満たされた
  3. 次の山場候補:
     - **T9 Phase 1 仕上げ** : ゴール条件チェック、バランス検証、KNOWLEDGE 蓄積
     - **T4-4 バランス調整** : Scout 救済、先攻有利問題、製造コスト/報酬の数値見直し
     - **T7-1 スキップ進行** : 「次のイベントが起きる日まで自動進行」(HoI4 風)
     - **モジュール追加 UI** : 可変モジュールスロットの add/remove

---

## Phase 1 のゴール（完了条件）
SPEC §1.6 より：
1. ✅ 車体・主砲・砲塔・エンジン・装甲・サスペンションを選んで戦車を組める（アセンブルタブ）
2. ✅ 組んだ戦車のステータスが 8 項目（初期は 5 項目）で算出される（リアルタイム）
3. ✅ ターン制シミュレーションの自動戦闘で性能差が確認でき、戦闘ログが出る（戦闘デモ + 任務）
4. ✅ セーブ／ロードできる（user://designs.json + user://gamestate.json）

→ **Phase 1 形式上クリア**。出撃モード（手動操作）・他兵科・ボス戦は Phase 2 以降。
→ 残るのは数値バランス調整（T4-4）と Scout 救済（KNOWLEDGE 参照）、品質確認（T9）。

---

## T1. 環境構築・基盤
- [x] T1-1：Godot 4 プロジェクトを作成し、Git リポジトリを初期化（既存 origin に最初のプロジェクトコミットを追加）
- [x] T1-2：フォルダ構成を決める（`data/`, `data/parts/`, `scenes/`, `scripts/`, `saves/`, `assets/`, `docs/` の分離。.gitkeep 配置）
- [~] T1-3：Claude Code と Codex の GDScript 出力品質を簡単なサンプルで検証し、相互レビュー運用を確立（SPEC §2.8 の留意点）
  - 運用ルール → `docs/REVIEW_FLOW.md`
  - 最小サンプル → `scenes/main.tscn` + `scripts/main.gd`
  - Claude Code 単独でのヘッドレス起動 + Godot エディタ F5 起動の両方で動作確認済み
  - **残：Codex 側レビュー（Codex 契約後に実施。それまではブロック中）**
- [x] T1-4：マルチプレイ将来対応のため、ユニット・戦闘状態を**シリアライズ可能なデータ構造**で設計する方針を確認（SPEC §2.14）
  - 成果物 → `docs/DATA_STRUCTURE.md`

## T2. データ構造（兵科非依存の共通基盤）
SPEC §3・§1.4 より。ここを汎用に作ることが後の兵科追加のカギ。
- [x] T2-1：ユニット定義のデータ構造（区分＋スロット＋装着パーツ＋算出ステータス）→ `scripts/unit.gd`
- [x] T2-2：パーツカタログを外部ファイル（JSON 等）で管理する仕組み → `scripts/parts_catalog.gd` + `data/parts/*.json` (7ファイル / 31パーツ)
- [x] T2-3：8 ステータスの定義（SPEC §2.1。初期は 5 項目：対人/対装甲/貫通/耐久/装甲）→ `scripts/stats.gd`
- [x] T2-4：スロット定義の枠組み（固定スロット＋可変モジュールスロット、SPEC §2.18）→ `Unit.FIXED_SLOTS` + `mod_*` モジュール群

## T3. ステータス計算エンジン
- [x] T3-1：装着パーツからユニットの各ステータスを算出するロジック → `Unit.compute_stats()`
- [~] T3-2：重量計算とトレードオフ（装甲↑で機動↓、車体サイズでモジュール枠決定。SPEC §2.18）
  - 重量集計は `Unit.total_weight()` で実装済み
  - 残：機動ステータス追加 (Phase 2)、モジュール枠数上限のチェック
- [~] T3-3：計算結果が手計算と一致するかのテスト（数値が正しいか自分で検証する手段）
  - 現状はメイン画面でデモ3設計の数値を出力。JSON を見れば足し算で検証可能
  - 残：自動テストの仕組み (任意。Phase 1 終盤で検討)

## T4. 車両パーツのデータ投入（Phase 1 = 3 区分）
SPEC §2.18 の確定スロット定義に基づく。具体的な数値は実装しながら調整。
- [x] T4-1：固定 6 スロットの選択肢データを作る（車体/主砲/砲塔/エンジン/装甲/サスペンション）
- [x] T4-2：現代/近未来の主砲データ（滑腔砲/機関砲/ATGM/榴弾砲/レールガン/レーザー）
- [x] T4-3：可変モジュールのデータ（副武装/追加装甲/無線機/暗視装置 等）
- [~] T4-4：3 区分（主力戦車 / 偵察戦闘車 / 対戦車ミサイル車両）の傾向が出るようパラメータ調整
  - Scout 救済済 (貫通失敗時の残存ダメ補正導入で 4 ダメ/R を確保)
  - 残：先攻有利の緩和、製造コストの細かい再調整、より多くのテストプレイ
- [ ] T4-5：仮の見た目（プレースホルダー画像 or 図形）でユニットを表示 → Phase 5 以降

## T5. アセンブル UI（司令官モードの中核）
参考：HoI4 タンクデザイナー（SPEC §2.17 ビジュアル方向性）
- [x] T5-1：スロット選択画面のレイアウト（TabContainer + HBox 3分割）
- [x] T5-2：パーツを選ぶと算出ステータスがリアルタイム更新される表示（_refresh_stats）
- [x] T5-3：組んだ車両に名前を付けて「設計」として保存する（user://designs.json）
- [x] T5-4：保存した設計の一覧・編集・削除（ItemList + 新規/保存/複製/削除ボタン）
  - 保存した設計は戦闘デモタブで自動的に総当たり 1v1 戦に投入される (最大 MAX_BATTLES=6 件)

## T6. 自動戦闘（ターン制シミュレーション）
SPEC §2.3 B・C より。
- [x] T6-1：戦闘の基本ループ（リスト順で 1 体ずつ行動。機動順は Phase 2 で導入予定）→ `scripts/combat.gd` Combat.run()
- [x] T6-2：攻撃解決（対人/対装甲の選択 → 貫通 vs 装甲 → ダメージ → 耐久減少。SPEC §2.2）→ Combat._attack()
  - 仮係数: 対人×0.5, 対装甲×0.4, 貫通失敗時さらに×0.25, 装甲閾値 25
- [ ] T6-3：射程による先制、索敵による有利の処理 → Phase 2 で着手（射程/索敵は 8 項目ステータスの未実装側）
- [x] T6-4：戦闘ログの出力（イベント Dictionary 列で記録、Combat.format_log で人間可読化）
- [x] T6-5：決着判定（全滅 or タイムアウト 30R）と戦果の集計

## T7. ゲーム進行（司令官モード）
SPEC §2.3 A・§2.4（資源）・§2.5（任務）より。
- [~] T7-1：日単位のターン進行と「スキップ進行」（イベント発生日まで自動で進む）
  - 「次の日へ」ボタンによる 1 日進行は実装済み
  - 残：スキップ進行（次のイベントが発生する日まで自動で進む）→ 任務実装後に有効化
- [x] T7-2：資源体系の最小実装（資金 + 素材の 2 種で確定 / SPEC §2.4 を Phase 1 暫定確定に更新）
- [x] T7-3：任務の最小実装（殲滅 × easy/medium/hard、SPEC §2.5 Phase 1 暫定確定）
  - `scripts/missions.gd`: 敵設計カタログ + 3 テンプレ + generate_one / resolve
  - 受注可能リストから選んで在庫ユニットを派遣 → 所要日数経過で自動戦闘 → 勝てば報酬・ユニット復帰、負けても復帰 (Phase 1)
  - 任務結果ログを直近 20 件 mission_log に保持
  - 残：失敗ペナルティ (戦死)、任務種別の追加、難易度バランス
- [x] T7-4：製造（設計を選んで素材+資金消費 → 在庫に追加。コスト = 重量 × 10 (資金) + 重量 × 5 (素材)）

## T8. セーブ／ロード
- [~] T8-1：ゲーム状態のセーブ（シリアライズ。マルチプレイ将来対応を意識。SPEC §2.7・§2.14）
  - 設計データの永続化は完了 (user://designs.json, scripts/save_io.gd)
  - 残：司令官モード実装時の「ゲーム全体の状態」セーブ (任務進行・資源・日付など)
- [x] T8-2：ロードと、セーブデータが壊れている場合のフォールバック
  - SaveIO.load_designs が null/配列でない/開けない を全て空配列で吸収
  - 空ならシード設計 (SEED_DESIGNS) で初期化する流れあり

## T9. Phase 1 仕上げ・検証
- [x] T9-1：ゴール条件（本ファイル冒頭の 4 点）が全て満たされているか確認 → 形式上 4/4 達成
- [ ] T9-2：自分でプレイして「設計の良し悪しが戦闘結果に反映されるか」を検証
  - ユーザー側で実機プレイが必要
- [ ] T9-3：バランスの明らかな破綻（最強パーツ一択など）がないか確認
  - 既知: Scout が誰にも勝てない (KNOWLEDGE 2026-06-19)、先攻有利 (同上)
  - 製造コストと任務報酬のバランス: 仮値 (Phase 1 暫定)
- [x] T9-4：詰まった点・学びを KNOWLEDGE.md に記録 → 6 件追記済み

---

## 後続フェーズ（Phase 1 完了後に詳細化、今は概要のみ）
- **Phase 2**：出撃モード（手動操作）追加。または兵科追加（ロボット前倒しも可）。SPEC §2.3 E 確定済
- **Phase 3**：他兵科・研究開発・ボス戦
- **Phase 4**：コアのクオリティアップ（バランス・コンテンツ）
- **Phase 5**：他人向け（チュートリアル・UI/UX 仕上げ・本番アセット）
- **Phase 6**：商用配信（配布・QA・ローカライズ・法的整備）

---

## P2. 出撃モード (Phase 2)
SPEC §2.3 E より。トップダウン 2D / WASD+マウス / ミッション固定マップ。
- [~] P2-1：コンバットフィールド最小実装（プレイヤー機体・敵 3 体静止・射撃・勝敗・戻り）
  - 実装済: scenes/combat_field.tscn + scripts/{player_tank,enemy_tank,projectile,combat_field}.gd
  - 司令官タブの「出撃テスト」ボタンで遷移
  - 残: 実機検証 (WASD 移動・マウス照準・左クリック射撃・敵撃破・勝利メッセージ)
- [ ] P2-2：敵 AI の最小実装（プレイヤー追跡 + 一定距離で射撃）
- [ ] P2-3：任務システムとの連携（手動で出撃 → 結果を任務に反映）
- [ ] P2-4：プレイヤー機体のステータスを保存済み設計から注入
- [ ] P2-5：マップに障害物・装飾を追加（cover 概念）

---

## 進め方のルール
1. タスクは上から順番が基本だが、T2〜T3（データ構造と計算）は他の土台なので最優先
2. 1 タスク完了ごとにこのファイルの記号を更新
3. 詰まったら `[!]` にして KNOWLEDGE.md に内容を書く
4. 数値バランスは「動かしてから調整」。最初から完璧を目指さない
5. SPEC の [TBD] にぶつかったら、その場で決めず SPEC 側に選択肢を残して相談する
